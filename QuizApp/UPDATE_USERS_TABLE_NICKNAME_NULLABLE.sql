-- ============================================
-- Users 테이블 nickname 컬럼을 nullable로 변경
-- Supabase SQL Editor에 복사해서 실행하세요
-- ============================================

-- nickname 컬럼을 nullable로 변경
ALTER TABLE users 
  ALTER COLUMN nickname DROP NOT NULL;

-- ============================================
-- 완료!
-- ============================================
-- 이제 회원가입 시 닉네임 없이도 users 테이블에 데이터를 저장할 수 있습니다.
-- 닉네임은 나중에 NicknameSetup 화면에서 설정할 수 있습니다.

